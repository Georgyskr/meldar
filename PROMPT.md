Read docs/BACKLOG.md Sprint 1 section. Each iteration: (1) Check current state — read BACKLOG.md for unchecked items, run existing tests, scan recent file changes. (2) Pick the next uncompleted Sprint 1 item top-to-bottom. (3) TDD workflow — write a failing test FIRST, verify it fails for the correct reason (not import error, not missing module, not phantom), then implement until green, then refactor. (4) After completing each logical unit (DB table, API route, UI component) run the full test suite and fix regressions before moving on. (5) Integration tests — use real Neon DB, mock external services (Resend, Vercel API, Anthropic API), never fire real calls to paid APIs in tests. (6) Mark items done in BACKLOG.md with [x] as you complete them. Test rules: zero per-assertion timeouts, one expect.timeout ceiling in test config, plain assertions everywhere else, more than 1 timeout per 100 tests means something is architecturally wrong so fix it. Every new function, route, or component gets a test before implementation, no exceptions. Code rules: follow CLAUDE.md strictly (Panda CSS, FSD layer imports, Text/Heading for typography, logical properties, zero comments by default, biome for lint/format). Do NOT commit or push git, files persist between iterations. When blocked: if you need credentials, design approval, or browser testing, write the blocker to docs/NEEDS_HUMAN.md with what is needed and why, then move to the next item. Output <promise>SPRINT1 SHIPPED</promise> when all Sprint 1 items that can be done without human input are implemented and all tests pass.
